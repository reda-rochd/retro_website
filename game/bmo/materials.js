// Minimal button material enhancement - use existing materials as-is
export function enhanceButtons({ three, renderer, plus, red, blue, green }) {
	// Buttons use their original materials from the model
	// No additional textures or bump maps needed
}

export default { enhanceButtons };
